# 評分報告 for TASK-002 (第 2 次循環 — 第 1 次返工結果)

## 評分時間
2026-05-28T21:30:00+08:00

## 評分者
reviewer-agent (REVIEWER 子代理)

---

## 評分檢查清單（必須 YES/NO）

| 檢查項目 | 判定 | 說明 |
|---------|------|------|
| 是否可執行 | **YES** | `seed_demo_data()` 可正常執行，pytest 10 項全部通過 (3.35s) |
| 是否有錯誤 | **NO** | 無錯誤；數值驗證一致（直接費 59,591,000 + 間接費 3,630,000 = 63,221,000 → 包商利潤 3,161,050 → 營業稅 3,319,102.5 → 總計 69,701,152.5） |
| 是否滿足需求條列 | **YES** | 三項需求（利潤計算修正、示範帳號按鈕、示範提示卡片）均已實作且持續正常 |
| 是否有測試或滿足審美 | **YES** | 新增 10 項 pytest 覆蓋利潤計算、資料結構、修改重算、資源建立等核心行為 |

---

## 評分明細

| 項目 | 分數 | 說明 |
|------|------|------|
| 完整性 (25分) | **25/25** | 全部 3 項需求均已完成，與第 1 次評分相同，無退化。 |
| 正確性 (25分) | **25/25** | 10 項 pytest 全部通過；數值驗證一致；`_recalc_budget_tree` 遞迴加總正確；`_apply_profit_rules` 百分比計算與順序 (profit → tax) 正確。 |
| 可維護性 (25分) | **25/25** | ✅ 前次缺失 2 已修復：`_recalc_seed` 已拆分為 `_recalc_budget_tree()`（通用遞迴）+ `_apply_profit_rules()`（利潤特殊邏輯）；✅ 前次缺失 3 已修復：建立 W 細項時的冗餘 `amount` 計算已移除（line 124/154 改為註解「amount 由 _recalc_seed 統一計算」）。 |
| 測試與驗證 (25分) | **25/25** | ✅ 前次缺失 1 已修復：新增 `api/test_seed_data_profit.py` 含 10 項測試案例，涵蓋使用者建立、專案建立、包商利潤金額驗證、營業稅金額驗證、總額一致性、樹狀深度 (≥3層)、葉節點金額 > 0、修改後重算正確性、項目數量 (≥25)、資源分類 (工/料/機)。測試架構使用 in-memory SQLite + 每測試重建 DB，隔離性良好。 |

---

## 總分

**100 / 100**

**結果：合格**

---

## 缺失項目

無。本次返工已完整修復第 1 次評分指出的兩項缺失：

| # | 前次缺失 | 修復狀態 | 證據 |
|---|---------|---------|------|
| 1 | seed_data 利潤計算無專屬測試 (15→25) | ✅ 已修復 | `api/test_seed_data_profit.py` 含 10 項測試 (all PASS) |
| 2 | 冗餘 amount 計算 + `_recalc_seed` 未拆分 (22→25) | ✅ 已修復 | 冗餘計算行已移除；`_recalc_seed` 拆分為 `_recalc_budget_tree` + `_apply_profit_rules` |

---

## 補充說明

1. **前端測試**：第 1 次評分的「次要問題 #4」（LoginPage / LandingPage 無前端測試）不在此次返工範圍內，且不影響核心功能的正確性。若後續需要補強，可另開任務處理。
2. **測試品質**：新測試案例使用類別名稱 + docstring 清晰標示測試目的（如 `TestProfitItemAmountCorrect`），遵循 Arrange-Act-Assert 模式，fixture 使用 `autouse=True` 的 `setup_db` 確保隔離。`TestRecalcAfterModify` 模擬真實編輯情境（修改 quantity → flush → recalc → refresh → 驗證子項與父項），覆蓋了 `_recalc_budget_tree` 的遞迴更新行為。
